mutation MoveCard($cardId: ID!, $newColumnId: ID!){
    changeCardColumnId(cardId: $cardId, newColumnId: $newColumnId){
            id,
            columnTitle,
            cards {
                id,
                cardText
            }
    }
}